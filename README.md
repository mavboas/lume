# lume
Lume is an AI-first backend language. Immutable by default, inferred types, errors as values, and one way to do everything. The syntax is designed to be concise enough for LLMs to generate with few tokens, and strict enough for the compiler to prove correctness.
